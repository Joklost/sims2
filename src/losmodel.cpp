
#include <sims2/losmodel.h>


const double sims2::losmodel::compute_los_pathloss(const double percentage) {
    return 0;
}

const std::vector<std::pair<int, int>>
sims2::losmodel::generate_pixel_coordinates(geo::Location pos1, geo::Location pos2) {
    return std::vector<std::pair<int, int>>();
}
